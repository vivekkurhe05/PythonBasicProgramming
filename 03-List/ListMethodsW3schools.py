# list(string) - splits string into a list of characters
# reverse() - reverses the list in place. Does not return anything. Overwrites the existing list
# sort() - sorts the list in place. Does not return anything. Overwrites the existing list
# sort(key=myFunc) - Sort the list by length of the values
# elem in list - returns true if list contains an elem. eg. "the" in
# pop(index) - removes the elem at the specified index and returns the deleted elem
# insert(index, elem) - Adds an element at the specified index
# append() - Adds an element at the end of the list
# + or extend() - concates two or more lists
# clear() - Removes all the elements from the list
# copy() - returns a copy of the list
# count() - returns the no. of elements with the specified value
# index() - returns the index of the first match, it raises an exception if no match is found
# remove(value) - removes the first match, it raises an exception if no match is found