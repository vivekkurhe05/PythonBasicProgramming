# Difference between pass and continue in python?

# ord() - returns unicode value of a character. It accepts character as a parameter
# chr() - converts unicode value into character. It accepts unicode value
# In JS, you use num.toFixed(2) to print number upto 2 decimal places.
# In Python, you use "{:.2f}".format(num)
# any() - returns true if any item in an iterable are true, otherwise false. Similar to some() in JS
# all() - returns true if all items in an iterable are true, otherwise false. Similar to every() in JS