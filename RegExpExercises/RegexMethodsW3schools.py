# findall(searchText, string) - It returns a list containing the exact matches. If no match is found, it returns empty list
# search(searchText, string) - It returns the match object. If no match is found, it returns None.
    # start() - It returns the index of the first match
    # end() - returns the last index of the match
    # span() - retuns a tuple containing start and end positions of the match
    # group() - returns a matched string
# finditer(searchText, string) - It returns the matched objects.
    # start() - returns the index of the first match
    # end() - returns the last index of the match
    # span() - retuns a tuple containing start and end positions of the match
    # group() - returns a matched string
# split(separator, string, maxsplit) - It splits a string into a list of substrings
# sub(oldstring, newstring, string, count) - replaces old string with a newstring upto the count (if specified).
# (?=\B[A-Z]) - to place cursor between two chars
# example - Use this string "PythonExercises" with above regular expression in regex101.com